# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=true
progress=false

# Registry settings
registry=https://registry.npmjs.org/

# Security: Prevent automatic execution of lifecycle scripts
ignore-scripts=false
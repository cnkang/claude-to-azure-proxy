{
  "name": "@repo/backend",
  "version": "2.0.0",
  "description": "Backend API service for Claude-to-Azure OpenAI proxy",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm run build:bundle",
    "build:with-types": "pnpm run build:types && pnpm run build:bundle",
    "build:types": "tsc --build --verbose",
    "build:bundle": "tsx ../../scripts/build-backend-bundle.ts",
    "build:tsc": "tsc --build --verbose",
    "build:clean": "tsc --build --clean && pnpm run build",
    "start": "node --enable-source-maps --max-old-space-size=512 --max-new-space-size=64 --gc-interval=100 --incremental-marking --concurrent-marking --parallel-scavenge dist/index.js",
    "start:prod": "node --enable-source-maps --max-old-space-size=1024 --max-new-space-size=128 --optimize-for-size --gc-interval=100 --incremental-marking --concurrent-marking --parallel-scavenge dist/index.js",
    "start:optimized": "node --enable-source-maps --max-old-space-size=2048 --max-new-space-size=256 --optimize-for-size --gc-interval=50 --incremental-marking --concurrent-marking --parallel-scavenge --expose-gc dist/index.js",
    "dev": "tsx watch --clear-screen=false src/index.ts",
    "dev:debug": "tsx watch --inspect=0.0.0.0:9229 src/index.ts",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --run --coverage",
    "test:smoke": "vitest --run tests/smoke.test.ts",
    "test:ui": "vitest --ui",
    "test:node24": "vitest --run --config vitest.config.ts",
    "test:performance": "vitest --run --config tests/performance/vitest.performance.config.ts",
    "test:memory": "node --expose-gc --max-old-space-size=512 node_modules/.bin/vitest --run --logHeapUsage",
    "test:benchmark": "vitest bench --reporter=verbose",
    "test:streaming-timeout": "tsx scripts/test-streaming-timeout.ts",
    "test:azure-direct": "tsx scripts/test-azure-direct.ts",
    "test:streaming-flow": "tsx scripts/test-streaming-flow-validation.ts",
    "monitoring:heap": "node --heap-prof --enable-source-maps dist/index.js",
    "monitoring:cpu": "node --cpu-prof --enable-source-maps dist/index.js",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "type-check": "tsc --noEmit --incremental",
    "type-check:watch": "tsc --noEmit --incremental --watch",
    "format": "prettier --write src/ tests/",
    "format:check": "prettier --check src/ tests/"
  },
  "dependencies": {
    "@repo/shared-types": "workspace:*",
    "@repo/shared-utils": "workspace:*",
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.0",
    "helmet": "^8.1.0",
    "joi": "^18.0.1",
    "multer": "2.0.2",
    "openai": "^6.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@repo/shared-config": "workspace:*",
    "@swc/cli": "^0.7.9",
    "@swc/core": "^1.15.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.10.0",
    "@types/superagent": "^8.1.9",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^4.0.7",
    "@vitest/ui": "^4.0.7",
    "supertest": "^7.1.4",
    "tsx": "^4.20.6",
    "typescript": "^5.6.0",
    "vitest": "^4.0.7"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
